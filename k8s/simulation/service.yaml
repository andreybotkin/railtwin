apiVersion: v1
kind: Service
metadata:
  name: simulation-service
  namespace: railway
  labels:
    app.kubernetes.io/name: simulation
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: simulation
    app.kubernetes.io/component: api
